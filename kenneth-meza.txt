https://codepen.io/kennymeza23/pen/bGwmLJa?editors=1100
1. What is Semantic HTML? 
        A semantic HTML is a tag that clearly describes what it's for.
2. What is HTML used for?
        HTML is used to structure the content on your webpage.
3. What is an attribute and where do we put it?
        An attribute is used to give more information about elements. They are put in the start tag.
4. What is the h1 tag used for? How many times should I use it on a page?
        The h1 tag is used to define the most important header of the page. It should be only used once.
5. Name two tags that have required attributes
        <a></a> and <img> have required attributes.
6. What do we put in the head of our HTML document?
        We put metadata in the head of our document. It doesn't show up on the webpage.
7. What is an id?
        An id specifies a unique id in HTML.
8. What elements can I add an id to?
        An id can be added to all HTML elements.
9. How many times can I use the same id on a page?
        You can only use the same id once on a page.
10. What is a class?
        A class specifies one or more classnames for an element.
11. What elements can I add a class to?
        You can add aa class to any HTML element.
12. How many times can I use the same class on a page?
        You can use the same class as many times as you want.
13. How do I get my link to open in a new tab?
        You add "_blank" to the <a href></a>.
14. What is the alt attribute in the image tag used for?
        It provides another way of describing the image in case one cannot see it.
15. How do I reference an id?
        You put #id to reference one.
16. What is the difference between a section and a div
        A section is a group of texts with a certaint theme and a div has no special meaning.
17. What is CSS used for?
    CSS is used to style a webpage.
18. How to we select an element? Example - every h2 on the page
        You put the element you want with brackets. ie: h2({}).
19. What is the difference between a class and an id? - Give me an example of when I might use each one
        An id can be used to identify one element and a class can identify multiple. ie: You'd use an id to specify the document l
20. How do we select classes in CSS?
        You put .class{}.
21. How do we select a p element with a single class of “human””?
        You would put .human p{}
22. What is a parent child selector? When would this be useful?
        It's a selector that selects all elements that are direct child of the specified element. 
23. How do you select all links within a div with the class of sidebar?
        You would put .sidebar a:link{}.
24. What is a pseudo selector?
        It's a selector used to define a special state of an element like whena user mouses over it.
25. What do we use the change the spacing between lines?
        You would use (line-height:)
26. What do we use to change the spacing between letters?
        You would use (letter-spacing:)
27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        You would use (text-transform:)
28. How do I add a 1px border around my div that is dotted and black?
        You would put (border: 1px dotted black;)
29. How do I select everything on the page? 
        The * selector selects everything
30. How do I write a comment in CSS?
        To write a comment you start with /* and end with */
31. How do I find out what file I am in, when I am using the command line?
        To find where you are you enter $ ls into the command line.
32. Using the command line - how do I see a list of files/folders in my current folder?
        You enter the command (dir) to see a list of files.
33. How do I remove a file via the command line? Why do I have to be careful with this?
        To remove a file you enter (git rm) and you have to be careful because you can delete everything.
34. Why should I use version control?
        You should use version control because it maintains a history of what changes happen to the content in Git.
35. How often should I commit to github?
        You should commit to Git as often as you can
36. What is the command we would use to push our repo up to github?
        git push -u will push your repo up to github.
37. Walk me through Lambda's git flow.
        Go to the project on github
        Fork the project
        Copy the URL
        Open your terminal
        Git clone [enter URL you copied]
        CD in the projectgit checkout -b "firstName-lastName"
        Code . (Opens VS Code)
        Finish project
        Save Your work in VS Code
        Git add .
        Git commit -m "Add commit messages here"
        Git push -u origin "firstName-lastName"
        Pull Request
        Go to YOUR FORKED repo on Github.
        Change branch from main to yours
        Click on pull Requestchange the base repository to your repo
        Submit pull Request
        DO NOT MERGE